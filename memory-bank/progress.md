# Progress

This file tracks the project's progress using a task list format.
2026-09-22 14:20:00 - Log of updates made.

## Completed Tasks

- [2026-09-22 14:20:00] Requirements analysis (requirements/req_en.md) and reference implementation review (scripts/template/interview_copilot.py, design.md, config.yaml).
- [2026-09-22 14:20:00] Architecture design — architecture.md created.
- [2026-09-22 14:20:00] Implementation plan — tasklist.md created (Phase 0–10, T0.1–T10.5).
- [2026-09-22 14:20:00] Memory Bank initialized.
- [2026-09-22 18:39:00] Phase 0 (Project bootstrap) complete — T0.1–T0.5.
- [2026-09-22 19:13:00] Phase 1 (Config core + skeleton UI) complete — T1.1–T1.8: `core/config.py` (ConfigManager: load + schema validation, atomic save, accessors; `validate_llm_entry`), `ui/js/index.html` (two tabs with all section shells + icon buttons), `ui/js/style.css` (base layout, multiline-10, icon-btn + CSS tooltips, modal/toast base), `ui/js/app.js` (tab switching + async get_config bridge probe), `ui/api.py` (get_config implemented + 15 stubs), `scripts/main/app.py` (entry: clear config failure, Api, webview window), `scripts/tests/test_config.py` (25 tests, all passing). GUI test verified headless via `scripts/dev/probe_ui.py` (PROBE OK: window opens, both tabs switch, status line shows parsed config — 2 LLMs, roles, hl, max_attempts).
- [2026-09-22 19:27:00] requirements.txt verified complete (user request): added PyQt6, PyQt6-WebEngine, qtpy (mandatory pywebview GUI backend — GTK unavailable) and pytest (tests). Verified via clean venv install of the updated file (all imports OK) + 25/25 tests.
- [2026-09-22 20:13:00] Phase 2 (Settings tab: LLM table + CRUD) complete — T2.1–T2.7 all done: `core/config.py` (ConfigManager LLM CRUD: list_llms/add_llm/update_llm/remove_llm with duplicate rejection + rename cascade + remove cascade to roles + last-LLM protection; role/HL accessors: get_roles/set_role/get_hl/set_hl), `ui/api.py` (implemented list_llms/save_llm/remove_llm/get_roles/set_role/get_hl/set_hl on top of ConfigManager; save_llm protocol: optional `_old_name` field distinguishes edit from add), `ui/js/index.html` (in-page LLM modal dialog with fields name/api_base/api_key/model/temperature + error div + save/cancel buttons), `ui/js/app.js` (Settings tab render: LLM table with row-click=select + row-dblclick=edit, Add/Edit/Remove buttons, role dropdowns + HL checkbox populated from LLM list, in-page LLM dialog with client-side validation + prefill on edit + `_old_name` sent on edit save, re-render after every mutation), `scripts/tests/test_config.py` (48 tests total: Part 1 Phase 1 + Part 2 Phase 2 — normalize_llm_entry, LLM CRUD incl. duplicate rejection/rename cascade/remove cascade/last-LLM rejection/atomicity, role/HL accessors). GUI test verified headless via `scripts/dev/probe_ui.py` (PROBE OK: scenarios 3.1–3.4 — Add dialog → Save → row appears + persisted; dblclick → prefill → change temp → Save → row + file updated; select + Remove with confirm → row gone + persisted; HL toggle → persisted as true).
- [2026-09-22 20:24:00] Bug fix: role dropdowns did not restore initial values from config.yaml on first render. Root cause: `renderRoleDropdowns()` in `app.js` only preserved the current `sel.value` (which was empty on first render from `initSettings`) instead of reading from `state.roles`. Fix: rewrote `renderRoleDropdowns()` to always restore values from `state.roles[roleKey]` (with existence check against `state.llms`). Added probe checks: `OK(roles-init)` (initial dropdown values match config.yaml) + `3.5 role change` (changing a role persists in the dropdown). 48/48 unit tests still pass; probe → PROBE OK.
- [2026-09-23 11:23:00] Feature: the Human-in-the-loop checkbox on the Prompts tab (`#hl-flag`) now mirrors the Settings flag (`#hl-flag-settings`) and pulls its value from config.yaml on app start. Root cause: `#hl-flag` was never touched by JS — only `hlFlagSettings` was. Fix: in `app.js` added `hlFlag = getElementById('hl-flag')` and synced it in `initSettings` (initial pull from config), `refreshSettings` (re-sync after any mutation), and `onHlChange` (keeps both in lock-step on toggle + error rollback). Also made the probe's HL scenario deterministic (set-to-opposite of the initial config value instead of hardcoding `true`) since the real config.yaml may have `hl: true` or `false`. Added probe checks: `OK(hl-init)` (initial Prompts HL checkbox matches config.yaml) + `OK(3.4b)` (Prompts HL checkbox syncs after Settings toggle). 48/48 unit tests pass; probe → PROBE OK.

## Current Tasks

- [2026-09-22 15:01:00] tasklist.md restructured: 9 phases (0–8), each ending with a manual GUI test; UI (skeleton window) moved up to Phase 1; dry-run Start in Phase 5; real auto run Phase 6; HL Phase 7; polish/regression Phase 8.

## Next Steps

- [2026-09-22 19:13:00] Phase 1 complete. Next: Phase 2 — Settings tab: LLM table + CRUD (T2.1–T2.7): ConfigManager LLM CRUD (list/add/update/remove with duplicate rejection + role cascade), role/HL accessors, Api list_llms/save_llm/remove_llm/get_roles/set_role, in-page LLM dialog (dialog.html/js), app.js Settings render + interactions, role dropdowns + HL checkbox, unit tests part 2 (duplicate rejection, remove cascades to roles, atomic write). GUI test: scenarios 3.1–3.4.
