# Prompt Optimizer configuration
# Seeded from scripts/template/config.yaml per architecture.md §6.
# llms[] — named LLM configurations (used in Settings tab table + role dropdowns)
# roles  — which LLM plays "LLM as Judge" (target executor) and
#          which LLM is "LLM for prompts" (ReAct agent / optimizer)
# hl     — Human-in-the-loop flag for the analyze_prompt step (2.1.3)
# max_attempts — cap on the optimization loop (llm1)

llms:
  - name: "qwen-judge"
    api_base: "http://192.168.1.4:1234/v1"
    api_key: "sk-9YYF0cHdAXvMNV6IqBQYC2cRekZLsNFP"
    model: "qwen/qwen3.8-27b"
    temperature: 0.0
  - name: "gemma-target"
    api_base: "http://192.168.1.57:1234/v1"
    api_key: "EMPTY"
    model: "google/gemma-4-12b-qat"
    temperature: 0.0

roles:
  judge: "qwen-judge"
  prompts: "gemma-target"

hl: false
max_attempts: 30
